version: "1.0"
status: DESIGN_ONLY
tests:
  - {id: SEC-AUTH-001, title: "MFA step-up required for publish", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "implementation admission", preconditions: "admin identity exists", attack_inputs: "password-only publish request", expected_result: "FAIL_CLOSED: publish denied without fresh MFA step-up", evidence_artifact: "auth-publish-mfa-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-002, title: "Separation of duties blocks self-publish", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "implementation admission", preconditions: "uploader and reviewer roles configured", attack_inputs: "same principal uploads reviews and publishes", expected_result: "FAIL_CLOSED: publish denied for conflicting principal", evidence_artifact: "auth-separation-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-003, title: "Reviewed hash change is rejected", owner: document-service, layer: INTEGRATION, phase: CORE, prompt_hint: "document lifecycle", preconditions: "version reviewed", attack_inputs: "replace object after review", expected_result: "FAIL_CLOSED: hash mismatch blocks publish", evidence_artifact: "review-hash-mismatch.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-004, title: "BOLA document read denied", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "resource authorization", preconditions: "two unrelated principals", attack_inputs: "foreign document identifier", expected_result: "FAIL_CLOSED: owner authorization denies foreign object", evidence_artifact: "bola-document-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-005, title: "Function authorization denied", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "admin authorization", preconditions: "non-publisher identity", attack_inputs: "direct publish function invocation", expected_result: "FAIL_CLOSED: function authorization denies action", evidence_artifact: "bfl-publish-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-006, title: "Session and idempotency replay denied", owner: identity-service, layer: SECURITY, phase: CORE, prompt_hint: "session implementation", preconditions: "expired and used nonces", attack_inputs: "replayed bearer context and idempotency key", expected_result: "FAIL_CLOSED: replay is rejected or safely no-op", evidence_artifact: "session-replay-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-007, title: "Forged request context denied", owner: identity-service, layer: CONTRACT, phase: CORE, prompt_hint: "context contract", preconditions: "signed context format", attack_inputs: "altered subject authorization revision or audience", expected_result: "FAIL_CLOSED: signature or binding verification rejects context", evidence_artifact: "context-forgery-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-008, title: "LATER OIDC JWT negative matrix", owner: identity-service, layer: SECURITY, phase: LATER, prompt_hint: "LATER live OIDC implementation", preconditions: "separately approved live issuer and audiences configured", attack_inputs: "wrong issuer audience alg none expired and key-use tokens", expected_result: "FAIL_CLOSED: every invalid JWT is rejected and live OIDC remains disabled without approval", evidence_artifact: "jwt-negative-matrix.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-009, title: "LATER JWKS SSRF denied", owner: identity-service, layer: SECURITY, phase: LATER, prompt_hint: "LATER live OIDC implementation", preconditions: "separately approved fixed JWKS profile", attack_inputs: "private IP redirect and attacker JWKS URI", expected_result: "FAIL_CLOSED: unapproved JWKS resolution is denied and Core has no remote resolution", evidence_artifact: "jwks-ssrf-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUTH-010, title: "Lateral workload call denied", owner: identity-service, layer: E2E, phase: CORE, prompt_hint: "workload identity", preconditions: "mTLS and service audiences", attack_inputs: "service token addressed to another service", expected_result: "FAIL_CLOSED: audience and ACL deny lateral call", evidence_artifact: "workload-lateral-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-001, title: "Polyglot MIME upload rejected", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "upload pipeline", preconditions: "quarantine configured", attack_inputs: "PDF executable polyglot", expected_result: "FAIL_CLOSED: upload remains quarantined and is rejected", evidence_artifact: "pdf-polyglot-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-002, title: "Malware scan blocks parse", owner: document-service, layer: INTEGRATION, phase: CORE, prompt_hint: "upload pipeline", preconditions: "AV verdict interface", attack_inputs: "malware-signature PDF", expected_result: "FAIL_CLOSED: no processing grant after malicious verdict", evidence_artifact: "pdf-av-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-003, title: "Object reference leak denied", owner: document-service, layer: SECURITY, phase: CORE, prompt_hint: "object authorization", preconditions: "scoped read grants", attack_inputs: "guessed object URL or prefix", expected_result: "FAIL_CLOSED: unscoped object reference cannot read content", evidence_artifact: "object-reference-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-004, title: "PDF resource bomb terminated", owner: processing-service, layer: SECURITY, phase: CORE, prompt_hint: "parser sandbox", preconditions: "sandbox limits configured", attack_inputs: "nested object expansion PDF", expected_result: "FAIL_CLOSED: parser is terminated within configured bounds", evidence_artifact: "pdf-bomb-limits.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-005, title: "Malformed parser exploit isolated", owner: processing-service, layer: SECURITY, phase: CORE, prompt_hint: "parser sandbox", preconditions: "no-network non-root sandbox", attack_inputs: "malformed exploit corpus", expected_result: "FAIL_CLOSED: no host write network egress or artifact publication", evidence_artifact: "parser-isolation-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PDF-006, title: "Immutable object overwrite denied", owner: document-service, layer: INTEGRATION, phase: CORE, prompt_hint: "object storage", preconditions: "write-once prefix", attack_inputs: "overwrite existing original or artifact key", expected_result: "FAIL_CLOSED: overwrite is denied and hash preserved", evidence_artifact: "object-immutable-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-001, title: "Corpus prompt injection remains data", owner: processing-service, layer: SECURITY, phase: CORE, prompt_hint: "chunk processing", preconditions: "untrusted-content classifier", attack_inputs: "document instructing system to ignore rules", expected_result: "FAIL_CLOSED: instruction is not executed and content is quarantined or treated as data", evidence_artifact: "corpus-injection-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-002, title: "Untrusted quality label rejected", owner: processing-service, layer: CONTRACT, phase: CORE, prompt_hint: "review metadata", preconditions: "review identity binding", attack_inputs: "forged approved label", expected_result: "FAIL_CLOSED: untrusted label cannot establish review state", evidence_artifact: "label-forgery-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-003, title: "Unpublished retrieval refused", owner: retrieval-service, layer: E2E, phase: CORE, prompt_hint: "eligibility gate", preconditions: "Document eligibility owner available", attack_inputs: "index hit for unpublished version", expected_result: "FAIL_CLOSED: retrieval returns refusal without evidence", evidence_artifact: "rag-unpublished-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-004, title: "Revocation race refused", owner: retrieval-service, layer: E2E, phase: CORE, prompt_hint: "eligibility epoch", preconditions: "epoch revocation simulation", attack_inputs: "revoke between search and response", expected_result: "FAIL_CLOSED: epoch mismatch returns refusal", evidence_artifact: "rag-revocation-race.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-005, title: "Eligibility owner outage refuses", owner: retrieval-service, layer: INTEGRATION, phase: CORE, prompt_hint: "failure handling", preconditions: "Document API unavailable", attack_inputs: "eligible index hit while owner unavailable", expected_result: "FAIL_CLOSED: no answer or evidence is returned", evidence_artifact: "rag-owner-outage.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-006, title: "Forged chunk lineage denied", owner: retrieval-service, layer: SECURITY, phase: CORE, prompt_hint: "context binding", preconditions: "opaque context verifier", attack_inputs: "altered document version chunk hash or context", expected_result: "FAIL_CLOSED: lineage mismatch is rejected", evidence_artifact: "chunk-lineage-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RAG-007, title: "Retrieval context replay denied", owner: retrieval-service, layer: SECURITY, phase: CORE, prompt_hint: "context binding", preconditions: "expired context", attack_inputs: "replay context under different request or subject", expected_result: "FAIL_CLOSED: context binding or expiry rejects replay", evidence_artifact: "retrieval-context-replay.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CITE-001, title: "Spoofed canonical citation denied", owner: citation-service, layer: SECURITY, phase: CORE, prompt_hint: "citation validation", preconditions: "owner APIs available", attack_inputs: "caller supplied title URL page and excerpt", expected_result: "FAIL_CLOSED: caller metadata is ignored and validation refuses", evidence_artifact: "citation-spoof-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CITE-002, title: "Missing citation blocks response", owner: citation-service, layer: E2E, phase: CORE, prompt_hint: "citation validation", preconditions: "substantive claim path", attack_inputs: "claim without canonical evidence", expected_result: "FAIL_CLOSED: substantive response is refused", evidence_artifact: "citation-missing-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CITE-003, title: "Citation source revocation recheck", owner: citation-service, layer: E2E, phase: CORE, prompt_hint: "citation validation", preconditions: "revoked source", attack_inputs: "opaque context referencing revoked source", expected_result: "FAIL_CLOSED: canonical recheck refuses rendering", evidence_artifact: "citation-revoked-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CITE-004, title: "Insufficiency override denied", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "chat assembly", preconditions: "retrieval insufficiency result", attack_inputs: "client flag requesting answer anyway", expected_result: "FAIL_CLOSED: structured refusal is returned", evidence_artifact: "insufficiency-override-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-OUT-001, title: "Markdown XSS rendered inert", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "output encoding", preconditions: "response renderer", attack_inputs: "script event-handler and data URL markdown", expected_result: "FAIL_CLOSED: unsafe markup is rejected or inert", evidence_artifact: "output-xss-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-OUT-002, title: "Unsafe URL denied", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "output encoding", preconditions: "URL policy", attack_inputs: "javascript file and credential URL", expected_result: "FAIL_CLOSED: unsafe URL is not emitted or navigable", evidence_artifact: "output-url-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-OUT-003, title: "Tool arguments never execute", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "output schema", preconditions: "strict output schema", attack_inputs: "model tool invocation payload", expected_result: "FAIL_CLOSED: tool arguments are rejected and no tool executes", evidence_artifact: "output-tool-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-001, title: "Provider IPv4 SSRF denied", owner: provider-service, layer: SECURITY, phase: LATER, prompt_hint: "provider admission", preconditions: "fixed endpoint profile", attack_inputs: "loopback private link-local and metadata IPv4", expected_result: "FAIL_CLOSED: endpoint activation and connection denied", evidence_artifact: "provider-ipv4-ssrf-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-002, title: "Provider IPv6 SSRF denied", owner: provider-service, layer: SECURITY, phase: LATER, prompt_hint: "provider admission", preconditions: "fixed endpoint profile", attack_inputs: "IPv6 loopback ULA link-local and mapped IPv4", expected_result: "FAIL_CLOSED: endpoint activation and connection denied", evidence_artifact: "provider-ipv6-ssrf-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-003, title: "Provider DNS CNAME escape denied", owner: provider-service, layer: SECURITY, phase: LATER, prompt_hint: "provider admission", preconditions: "DNS validator", attack_inputs: "allowlisted host CNAME to private address", expected_result: "FAIL_CLOSED: DNS chain validation denies connection", evidence_artifact: "provider-cname-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-004, title: "Provider redirect and rebinding denied", owner: provider-service, layer: SECURITY, phase: LATER, prompt_hint: "provider admission", preconditions: "connect-time resolver", attack_inputs: "public URL redirect or rebinding to private address", expected_result: "FAIL_CLOSED: redirect and changed resolved address denied", evidence_artifact: "provider-rebinding-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-005, title: "Provider proxy bypass denied", owner: provider-service, layer: E2E, phase: LATER, prompt_hint: "provider admission", preconditions: "egress proxy firewall", attack_inputs: "direct socket and proxy environment override", expected_result: "FAIL_CLOSED: only approved proxy route is usable", evidence_artifact: "provider-proxy-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-006, title: "Provider secret readback denied", owner: provider-service, layer: SECURITY, phase: LATER, prompt_hint: "provider secret", preconditions: "write-only secret reference", attack_inputs: "GET configuration and log exception", expected_result: "FAIL_CLOSED: plaintext key is never returned or logged", evidence_artifact: "provider-secret-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SSRF-007, title: "Single active provider race denied", owner: provider-service, layer: INTEGRATION, phase: LATER, prompt_hint: "provider activation", preconditions: "activation lock", attack_inputs: "concurrent activation requests", expected_result: "FAIL_CLOSED: no more than one active configuration commits", evidence_artifact: "provider-active-race.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-001, title: "PII canary redacted in logs traces audit", owner: privacy-owner, layer: SECURITY, phase: CORE, prompt_hint: "privacy implementation", preconditions: "redaction pipeline", attack_inputs: "PII and secret canaries through success and failure paths", expected_result: "FAIL_CLOSED: bodies and raw secrets are absent from logs traces and audit", evidence_artifact: "privacy-observability-canary.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-002, title: "PII canary excluded from event and DLQ", owner: privacy-owner, layer: INTEGRATION, phase: CORE, prompt_hint: "event privacy", preconditions: "sanitized envelope and DLQ", attack_inputs: "failed event containing PII canary", expected_result: "FAIL_CLOSED: raw sensitive body is absent from event and DLQ", evidence_artifact: "privacy-dlq-canary.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-003, title: "Provider content egress denied by default", owner: privacy-owner, layer: ADMISSION, phase: LATER, prompt_hint: "provider governance", preconditions: "no approved provider DPA", attack_inputs: "chat content export request", expected_result: "FAIL_CLOSED: content is NOT_SENT without approval evidence", evidence_artifact: "provider-egress-default-deny.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-004, title: "System prompt leakage denied", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "chat confidentiality", preconditions: "prompt boundary", attack_inputs: "prompt extraction and error probes", expected_result: "FAIL_CLOSED: system instructions and hidden context are not disclosed", evidence_artifact: "system-prompt-leak-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-005, title: "Deletion propagates through cache and projections", owner: privacy-owner, layer: E2E, phase: CORE, prompt_hint: "deletion implementation", preconditions: "tombstone ledger", attack_inputs: "subject deletion request", expected_result: "FAIL_CLOSED: deleted data is unavailable in owner stores projections and cache", evidence_artifact: "deletion-propagation.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-006, title: "Backup restore reapplies deletion ledger", owner: privacy-owner, layer: E2E, phase: LATER, prompt_hint: "separate approved backup/DR implementation", preconditions: "separately approved backup/DR and tombstone ledger", attack_inputs: "restore backup containing expired or deleted record", expected_result: "FAIL_CLOSED: restored record cannot become serveable", evidence_artifact: "restore-deletion-ledger.json", status: PLANNED_NOT_RUN}
  - {id: SEC-PRIV-007, title: "Encryption key scope enforced", owner: platform-security, layer: SECURITY, phase: CORE, prompt_hint: "KMS implementation", preconditions: "KMS aliases and service principals", attack_inputs: "wrong service principal and key export attempt", expected_result: "FAIL_CLOSED: unauthorized decrypt or KEK export denied", evidence_artifact: "kms-scope-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-001, title: "Forged event producer denied", owner: platform-security, layer: SECURITY, phase: CORE, prompt_hint: "broker security", preconditions: "mTLS broker ACL", attack_inputs: "unauthorized producer certificate", expected_result: "FAIL_CLOSED: broker and consumer reject producer", evidence_artifact: "event-producer-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-002, title: "Duplicate and altered replay handled", owner: platform-security, layer: INTEGRATION, phase: CORE, prompt_hint: "inbox implementation", preconditions: "inbox and payload hash", attack_inputs: "same message ID same and altered hash", expected_result: "FAIL_CLOSED: duplicate is no-op and altered replay quarantined", evidence_artifact: "event-replay-integrity.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-003, title: "Out-of-order and gap event handled", owner: platform-security, layer: INTEGRATION, phase: CORE, prompt_hint: "revision fencing", preconditions: "aggregate revisions", attack_inputs: "stale and gap revisions", expected_result: "FAIL_CLOSED: stale is no-op and gap reconciles without unsafe state", evidence_artifact: "event-order-gap.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-004, title: "Event payload hash mismatch denied", owner: platform-security, layer: CONTRACT, phase: CORE, prompt_hint: "event contract", preconditions: "canonical hash verifier", attack_inputs: "modified data with original hash", expected_result: "FAIL_CLOSED: integrity mismatch is rejected", evidence_artifact: "event-hash-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-005, title: "Poison DLQ has no automatic replay", owner: platform-security, layer: E2E, phase: CORE, prompt_hint: "DLQ implementation", preconditions: "DLQ policy", attack_inputs: "malformed unauthorized event", expected_result: "FAIL_CLOSED: event quarantines and never auto-replays", evidence_artifact: "event-dlq-no-replay.json", status: PLANNED_NOT_RUN}
  - {id: SEC-EVT-006, title: "Unauthorized DLQ replay denied", owner: platform-security, layer: SECURITY, phase: CORE, prompt_hint: "DLQ implementation", preconditions: "replay authorization", attack_inputs: "unprivileged replay request", expected_result: "FAIL_CLOSED: replay request is denied and audited", evidence_artifact: "event-replay-auth-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-DB-001, title: "Cross-service database read denied", owner: platform-security, layer: SECURITY, phase: CORE, prompt_hint: "data isolation", preconditions: "per-service credentials", attack_inputs: "retrieval principal SQL to Document database", expected_result: "FAIL_CLOSED: database authentication and network policy deny access", evidence_artifact: "db-cross-service-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-DB-002, title: "Object prefix isolation denied", owner: platform-security, layer: SECURITY, phase: CORE, prompt_hint: "object-prefix isolation", preconditions: "scoped object-prefix roles", attack_inputs: "foreign object-prefix list or read", expected_result: "FAIL_CLOSED: foreign object-prefix access is denied", evidence_artifact: "storage-cross-service-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-DB-003, title: "At-rest encryption and key separation verified", owner: platform-security, layer: ADMISSION, phase: CORE, prompt_hint: "storage admission", preconditions: "KMS configuration evidence", attack_inputs: "unencrypted volume or shared key configuration", expected_result: "FAIL_CLOSED: deployment admission denies nonconforming storage", evidence_artifact: "storage-encryption-admission.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUD-001, title: "Audit tamper detected", owner: audit-service, layer: SECURITY, phase: CORE, prompt_hint: "audit implementation", preconditions: "hash chain checkpoint", attack_inputs: "modified historical audit record", expected_result: "FAIL_CLOSED: verification detects tamper and alerts", evidence_artifact: "audit-tamper-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUD-002, title: "Audit gap detected", owner: audit-service, layer: INTEGRATION, phase: CORE, prompt_hint: "audit implementation", preconditions: "sequence checkpoint", attack_inputs: "deleted record or missing checkpoint", expected_result: "FAIL_CLOSED: gap is detected and privileged action is blocked or alerted", evidence_artifact: "audit-gap-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-AUD-003, title: "Privileged audit read and signing key isolated", owner: audit-service, layer: SECURITY, phase: CORE, prompt_hint: "audit implementation", preconditions: "separate audit trust domain", attack_inputs: "application principal audit query and signing-key request", expected_result: "FAIL_CLOSED: unauthorized read and key access denied", evidence_artifact: "audit-privileged-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SC-001, title: "Dependency and image critical finding blocks admission", owner: platform-security, layer: ADMISSION, phase: BOTH, prompt_hint: "supply chain admission", preconditions: "scanner policy", attack_inputs: "artifact with Critical or High finding", expected_result: "FAIL_CLOSED: build or deploy admission is blocked", evidence_artifact: "supply-critical-admission.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SC-002, title: "Unsigned parser or model artifact denied", owner: platform-security, layer: ADMISSION, phase: BOTH, prompt_hint: "supply chain admission", preconditions: "provenance and signature verifier", attack_inputs: "unsigned or wrong-digest parser model artifact", expected_result: "FAIL_CLOSED: artifact is not admitted", evidence_artifact: "supply-signature-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SC-003, title: "Unpinned CI action denied", owner: platform-security, layer: ADMISSION, phase: BOTH, prompt_hint: "supply chain admission", preconditions: "CI policy", attack_inputs: "floating action tag and missing SBOM", expected_result: "FAIL_CLOSED: pipeline admission is blocked", evidence_artifact: "ci-provenance-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-REL-001, title: "Candidate and provider activation need human approval", owner: release-owner, layer: ADMISSION, phase: BOTH, prompt_hint: "release gate", preconditions: "approval record absent", attack_inputs: "automatic candidate or provider activation", expected_result: "FAIL_CLOSED: activation does not occur", evidence_artifact: "release-human-approval-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-REL-002, title: "Critical High evidence gate blocks deploy", owner: release-owner, layer: ADMISSION, phase: BOTH, prompt_hint: "release gate", preconditions: "linked test not measured", attack_inputs: "deployment request with missing evidence", expected_result: "FAIL_CLOSED: capability activation is blocked", evidence_artifact: "release-evidence-gate-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-REL-003, title: "Kill switch disables candidate", owner: release-owner, layer: E2E, phase: BOTH, prompt_hint: "release gate", preconditions: "candidate active in test environment", attack_inputs: "authorized kill switch request", expected_result: "FAIL_CLOSED: candidate traffic stops and rollback state records", evidence_artifact: "release-killswitch.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RES-001, title: "Parser resource limits enforced", owner: processing-service, layer: SECURITY, phase: CORE, prompt_hint: "resource limits", preconditions: "sandbox limits", attack_inputs: "CPU RAM page output and time exhaustion PDF", expected_result: "FAIL_CLOSED: bounded termination with no publication", evidence_artifact: "resource-parser-limits.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RES-002, title: "Request token retrieval budget enforced", owner: api-gateway, layer: E2E, phase: CORE, prompt_hint: "resource limits", preconditions: "gateway budgets", attack_inputs: "oversized question tokens and retrieval fanout", expected_result: "FAIL_CLOSED: request is rate limited or refused within budget", evidence_artifact: "resource-request-budget.json", status: PLANNED_NOT_RUN}
  - {id: SEC-RES-003, title: "Queue storage cost overload contained", owner: api-gateway, layer: E2E, phase: CORE, prompt_hint: "resource limits", preconditions: "queue and storage quotas", attack_inputs: "sustained enqueue and upload flood", expected_result: "FAIL_CLOSED: quotas shed work without unbounded retry or spend", evidence_artifact: "resource-queue-storage-budget.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CACHE-001, title: "Cross-user cache disclosure denied", owner: chat-service, layer: SECURITY, phase: CORE, prompt_hint: "cache isolation", preconditions: "subject-bound cache key", attack_inputs: "second user requests first user cache key", expected_result: "FAIL_CLOSED: no foreign cached content is returned", evidence_artifact: "cache-cross-user-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-CACHE-002, title: "Cache invalidates on revocation deletion", owner: chat-service, layer: INTEGRATION, phase: CORE, prompt_hint: "cache invalidation", preconditions: "tombstone and revocation events", attack_inputs: "request after revocation or deletion", expected_result: "FAIL_CLOSED: cache entry is invalidated and response refused", evidence_artifact: "cache-invalidation-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SOURCE-001, title: "Future connector private target denied", owner: document-service, layer: SECURITY, phase: LATER, prompt_hint: "source connector admission", preconditions: "connector remains disabled", attack_inputs: "third-party API private IP endpoint", expected_result: "FAIL_CLOSED: connector route and egress are denied", evidence_artifact: "source-private-target-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SOURCE-002, title: "Future connector write operation denied", owner: document-service, layer: CONTRACT, phase: LATER, prompt_hint: "source connector admission", preconditions: "read-only allowlist", attack_inputs: "CREATE UPDATE DELETE unknown operation", expected_result: "FAIL_CLOSED: operation is denied", evidence_artifact: "source-write-negative.json", status: PLANNED_NOT_RUN}
  - {id: SEC-SOURCE-003, title: "Future connector credential excluded", owner: document-service, layer: SECURITY, phase: LATER, prompt_hint: "source connector admission", preconditions: "secret reference design", attack_inputs: "source credential in payload log or error", expected_result: "FAIL_CLOSED: raw credential is absent and request denied", evidence_artifact: "source-secret-negative.json", status: PLANNED_NOT_RUN}
