{
  "format": "service-ownership-v1",
  "source": "docs/architecture/container-map.md",
  "components": [
    {"id": "api-gateway", "kind": "service", "path": "services/api-gateway", "bounded_context": "Transport routing and verified auth context", "status": "CORE", "data_ownership": "Self-only transport policy state; no business data", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "identity-service", "kind": "service", "path": "services/identity-service", "bounded_context": "Identity, credentials, sessions, roles, permissions, and service identity", "status": "CORE", "data_ownership": "Self-only identity and access data", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "audit-service", "kind": "service", "path": "services/audit-service", "bounded_context": "Append-only audit integrity, retention, and query", "status": "CORE", "data_ownership": "Self-only append-only audit data", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "document-service", "kind": "service", "path": "services/document-service", "bounded_context": "Immutable document/version and canonical legal metadata", "status": "CORE", "data_ownership": "Self-only document, version, and canonical metadata", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "processing-service", "kind": "service", "path": "services/processing-service", "bounded_context": "Extraction, normalization, chunks, locators, and quality review", "status": "CORE", "data_ownership": "Self-only processing jobs and derived artifact references", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "index-service", "kind": "service", "path": "services/index-service", "bounded_context": "FTS projection and index lifecycle", "status": "CORE", "data_ownership": "Self-only index projections and index state", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "retrieval-service", "kind": "service", "path": "services/retrieval-service", "bounded_context": "Query normalization, retrieval runs, ranking, and sufficiency", "status": "CORE", "data_ownership": "Self-only retrieval runs and configuration", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "citation-service", "kind": "service", "path": "services/citation-service", "bounded_context": "Citation validation and canonical source rendering", "status": "CORE", "data_ownership": "Self-only citation validation runs", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "chat-service", "kind": "service", "path": "services/chat-service", "bounded_context": "Conversation, answer/refusal snapshots, and idempotency", "status": "CORE", "data_ownership": "Self-only conversation and immutable answer/refusal snapshots", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "provider-service", "kind": "service", "path": "services/provider-service", "bounded_context": "Provider configuration, secret references, lifecycle, and adapter", "status": "LATER", "data_ownership": "Self-only provider configuration and secret references", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "feedback-service", "kind": "service", "path": "services/feedback-service", "bounded_context": "Feedback, annotation, and Golden Answer lifecycle", "status": "LATER", "data_ownership": "Self-only feedback, annotation, and Golden Answer data", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "evaluation-service", "kind": "service", "path": "services/evaluation-service", "bounded_context": "Frozen datasets, evaluation runs, metrics, and candidate gate", "status": "LATER", "data_ownership": "Self-only dataset, evaluation, and candidate metadata", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "web-chat", "kind": "app", "path": "apps/web-chat", "bounded_context": "Web Chat client", "status": "CLIENT", "data_ownership": "Self-only client-local state; no service-owned data", "allowed_workspace_dependencies": ["packages/generated-contracts"]},
    {"id": "admin-portal", "kind": "app", "path": "apps/admin-portal", "bounded_context": "Admin Portal client", "status": "CLIENT", "data_ownership": "Self-only client-local state; no service-owned data", "allowed_workspace_dependencies": ["packages/generated-contracts"]}
  ]
}
