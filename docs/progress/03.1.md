# Báo cáo Prompt 03.1 — monorepo repository/build-boundary skeleton

## Trạng thái đề xuất

`PASS_CANDIDATE`

## Đã thay đổi

- Tạo đúng 12 service skeleton trong `services/` và 2 client-app skeleton trong `apps/`. Mỗi component chỉ có README và manifest TOML cục bộ; không có runtime, route, model, migration, cấu hình hay secret.
- Tạo `packages/generated-contracts/` là boundary metadata-only. `contracts-index.json` kiểm chứng 188 file canonical trong `contracts/` với SHA-256 tree `b3be7a19c7d2fcf88706c08c760f5ec4cb00f391bbda2ff2aa9d48b705898eb6`; không sửa `contracts/`.
- Tạo `packages/workspace-tooling/workspace.py`, công cụ Python standard-library fail-closed cho `bootstrap`, `lint`, `test`, `build`, `build-all` và `verify`. Targeted `build <component>` chỉ xác thực target cùng generated-contract boundary và canonical `contracts/`, không yêu cầu sibling service/app hoặc ownership document.
- Tạo `docs/architecture/service-ownership.yaml` (JSON hợp lệ YAML) có parity với inventory và container map: 9 service `CORE`, 3 service `LATER`, 2 client `CLIENT` không được container map gán trạng thái Core/LATER.
- Thêm `Makefile`, boundary placeholder tối thiểu cho `infra/` và `deploy/`, unit coverage và machine-readable validation/preservation evidence tại `tests/evidence/03.1-validation.json` và `tests/evidence/03.1-preservation.json`.

## Bằng chứng

Kết quả lệnh thực tế, exit code và `NOT_MEASURED` được lưu máy đọc được tại `tests/evidence/03.1-validation.json`. Bảo toàn tracked repository file được ghi riêng tại `tests/evidence/03.1-preservation.json`, đối chiếu immutable HEAD `638ea5112923cc9448599f0ad18d87d7e2623678`; không tuyên bố hash working tree trước prompt vì chưa đo.

- `python packages\workspace-tooling\workspace.py bootstrap` — exit 0.
- `python packages\workspace-tooling\workspace.py lint` — exit 0.
- `python packages\workspace-tooling\workspace.py test` — exit 0; 6 test skeleton pass.
- `python packages\workspace-tooling\workspace.py build-all --output-dir deploy\artifacts` — exit 0; 14 ZIP artifacts.
- `python -m unittest tests.test_monorepo_skeleton.MonorepoSkeletonTests.test_targeted_build_succeeds_in_isolated_workspace` — exit 0; 1 test copy tool, canonical contracts, generated-contract package và đúng một service vào temporary workspace không có sibling/app/ownership document, rồi target build pass.
- Vòng lặp explicit gọi `build <component-path>` cho đủ 12 services và 2 apps — exit 0; 14 targeted build pass.
- `python -m unittest tests/test_monorepo_skeleton.py` — exit 0; 6 test pass.
- `python -m unittest tests/test_prompt_gate_lifecycle.py` — exit 0; 26 test pass.
- `python scripts\verify_pack.py` — exit 0; `PASS: 72 commands, configs, manifest/state structure, and revision governance lifecycle are valid`.
- `git diff --check` — exit 0.
- Git preservation checks (`git rev-parse HEAD`, `git diff --name-status 638ea5112923cc9448599f0ad18d87d7e2623678 --`, `git status --short`) — exit 0; chỉ `.agent-run/prompt-state.json` do gate và pre-existing deletion ZIP xuất hiện như tracked changes.
- `where make` — exit 1; local shell confirmed `make` is unavailable.
- `make bootstrap`, `make lint`, `make test`, `make build` — `NOT_MEASURED`: local Windows lacks `make`; targets were deliberately not executed.

## Đối chiếu nguyên tắc cứng

- HR-01/HR-02: chỉ Prompt 03.1 được thực hiện; không có approve/reject/defer, commit, push hoặc prompt tiếp theo.
- HR-03/HR-04/HR-10: chỉ dựng repository boundary theo hướng đã duyệt; không có business code, contract, API, event, schema, stack version hay dependency mới.
- HR-06: chỉ ghi trong write scope được cấp (bao gồm `apps/` theo amendment); preservation evidence phân loại `.agent-run/prompt-state.json` là gate-managed và ZIP deletion là pre-existing, không restore/stage/tác động. Tooling không cung cấp cleanup/delete operation.
- HR-07/HR-13: manifest local, ownership metadata, inventory and artifact-isolation tests truy vết boundary; chỉ `packages/generated-contracts` được phép, không có shared business/runtime package.
- HR-23/HR-36/HR-38: không thêm secret/config runtime; tooling chỉ dùng Python stdlib, kiểm tra fail-closed và artifact metadata deterministic.
- HR-39/HR-41: isolated-target regression, 14 targeted builds, existing lifecycle test, pack verification, whitespace và Git preservation checks đã chạy; evidence ghi kết quả thực tế.

## Sai lệch, rủi ro và điều kiện còn lại

- Make targets là `NOT_MEASURED` vì `make` không có trong môi trường Windows cục bộ. Đây không phải claim build bằng Make.
- Skeleton không chứng minh FastAPI/Next.js runtime, dependency pinning, deployment, CI, database, broker hay provider capability; các phạm vi này không được phép trong Prompt 03.1.
- `deploy/artifacts/` là output ignored của tooling và được giữ lại; không có cleanup/delete command trong tooling và không có artifact nào được stage.

## Đề xuất prompt tiếp theo

Chỉ sau human approval cho Prompt 03.1, đề xuất **Prompt 03.2 — Runtime template cho backend service**. Prompt 03.2 không được chạy trong phiên này.
