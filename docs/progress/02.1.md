# Progress report — Prompt 02.1

## Trạng thái đề xuất

`PASS_WITH_CONDITIONS_CANDIDATE` cho Prompt 02.1. Mô hình có đúng 12 bounded context, một owner cho mỗi capability, không cross-DB và synchronous DAG không chu trình. `DEC-005` còn **OPEN**; cần separately approved governance/manifest resolution trước khi claim executable Core path.

Không có direct/manual state edit; legitimate `python scripts/prompt_gate.py start 02.1` đã thay đổi gate-managed state thành `IN_PROGRESS`. Follow-up này không sửa `.agent-run/` hay bất kỳ state file nào.

## Đã thay đổi

- `docs/architecture/container-map.md` — Bản đồ 12 logical container, API/event capability table, dependency DAG và Core fail-closed flow.
- `docs/architecture/data-ownership.md` — Capability matrix và data ownership cho 12 container.
- `docs/progress/02.1.md` — Báo cáo này.

Không thay đổi source, contract, migration, schema hay manifest và không có direct/manual state edit; legitimate gate `start 02.1` đã đổi `.agent-run/prompt-state.json` thành `IN_PROGRESS`. Mọi tài liệu prior ngoài scope và ZIP do người dùng xóa được bảo toàn.

## Bằng chứng

Validation owner là Orchestrator: Orchestrator sẽ đọc lại, validate độc lập và submit; writer không chạy submit/approve/reject/defer/next prompt.

### A. Whitespace diff

```cmd
git diff --check
```

Exit code: `0`.

Actual output:

```text
(no output)
```

### B. Container, capability và synchronous-DAG parser

```cmd
python -c "from pathlib import Path; from graphlib import TopologicalSorter; import json; p=Path('docs/architecture/container-map.md'); s=p.read_text(encoding='utf-8'); expected=['api-gateway','identity-service','audit-service','document-service','processing-service','index-service','retrieval-service','provider-service','citation-service','chat-service','feedback-service','evaluation-service']; table=lambda h,n:[r.split('|')[1:] for r in s.split(h,1)[1].split(n,1)[0].splitlines() if r.startswith('|')][2:]; containers=[r[0].strip() for r in table('## Phạm vi và trạng thái quyết định','API capability')]; capabilities=[r[0].strip() for r in table('## Provided API and event capabilities','## Allowed synchronous dependencies')]; sync=table('## Allowed synchronous dependencies','## Chứng minh đồ thị không chu trình'); edges=[(r[0].strip(),r[1].strip()) for r in sync]; assert containers==expected and capabilities==expected and len(sync)==21 and len(set(edges))==21 and not any(a==b for a,b in edges); graph={n:set() for n in expected}; [graph[b].add(a) for a,b in edges]; topo=list(TopologicalSorter(graph).static_order()); assert all(topo.index(a)<topo.index(b) for a,b in edges); print(json.dumps({'containers':len(containers),'capability_rows':len(capabilities),'nodes':len(graph),'edges':len(edges),'duplicates':len(edges)-len(set(edges)),'self_edges':sum(a==b for a,b in edges),'acyclic':True,'topological_order':topo},ensure_ascii=True))"
```

Exit code: `0`.

Actual output:

```json
{"containers": 12, "capability_rows": 12, "nodes": 12, "edges": 21, "duplicates": 0, "self_edges": 0, "acyclic": true, "topological_order": ["api-gateway", "identity-service", "audit-service", "evaluation-service", "feedback-service", "chat-service", "retrieval-service", "provider-service", "citation-service", "index-service", "processing-service", "document-service"]}
```

### C. Capability ownership parser

```cmd
python -c "from pathlib import Path; import json; s=Path('docs/architecture/data-ownership.md').read_text(encoding='utf-8'); expected={'api-gateway','identity-service','audit-service','document-service','processing-service','index-service','retrieval-service','provider-service','citation-service','chat-service','feedback-service','evaluation-service'}; table=lambda h,n:[r.split('|')[1:] for r in s.split(h,1)[1].split(n,1)[0].splitlines() if r.startswith('|')][2:]; caps=[tuple(x.strip() for x in r[:3]) for r in table('## Capability ownership matrix','## Service data ownership')]; services=[r[0].strip() for r in table('## Service data ownership','## Quy tắc dữ liệu và điều kiện mở')]; ids=[r[0] for r in caps]; owners=[r[2] for r in caps]; required=['Edge routing','Identity/authentication','Audit','Legal metadata','Original object','Processing jobs','Extracted/chunk','Quality review','Index projections','Retrieval/ranking','Citation validation','Conversations','Provider config','Feedback','Annotation','Golden Answer','Frozen datasets','Evaluation runs','Candidates','Optional training export']; missing=[x for x in required if not any(x in r[1] for r in caps)]; duplicate=sorted({x for x in ids if ids.count(x)>1}); assert len(caps)==20 and len(ids)==len(set(ids)) and all(x and ' ' not in x and x.startswith('CAP-') for x in ids) and all(x in expected for x in owners) and set(services)==expected and len(services)==12 and not missing; print(json.dumps({'capability_rows':len(caps),'unique_capability_ids':len(set(ids)),'owners':sorted(set(owners)),'service_ownership_rows':len(services),'missing':missing,'duplicate_capability_ids':duplicate},ensure_ascii=True))"
```

Exit code: `0`.

Actual output:

```json
{"capability_rows": 20, "unique_capability_ids": 20, "owners": ["api-gateway", "audit-service", "chat-service", "citation-service", "document-service", "evaluation-service", "feedback-service", "identity-service", "index-service", "processing-service", "provider-service", "retrieval-service"], "service_ownership_rows": 12, "missing": [], "duplicate_capability_ids": []}
```

### D. Static invariant scan

```cmd
python -c "from pathlib import Path; import json; s=Path('docs/architecture/container-map.md').read_text(encoding='utf-8')+'\n'+Path('docs/architecture/data-ownership.md').read_text(encoding='utf-8'); checks={'cross_service_db_prohibition':'Không cross-service DB','shared_business_model_prohibition':'shared business model','gateway_no_business_orchestration_aggregation':'Gateway no business orchestration','citation_independent_provider':'độc lập kiến trúc với Provider','opaque_request_bound_retrieval_context':'opaque retrieval-context reference bound to request','dec_005_open':'DEC-005` vẫn **OPEN**','core':'| Core |','later':'| LATER |'}; missing=[name for name,needle in checks.items() if needle not in s]; assert not missing; print(json.dumps({'checked':list(checks),'missing':missing},ensure_ascii=True))"
```

Exit code: `0`.

Actual output:

```json
{"checked": ["cross_service_db_prohibition", "shared_business_model_prohibition", "gateway_no_business_orchestration_aggregation", "citation_independent_provider", "opaque_request_bound_retrieval_context", "dec_005_open", "core", "later"], "missing": []}
```

### E. Prompt gate status

```cmd
python scripts/prompt_gate.py status --all
```

Exit code: `0`.

Actual output summary: `01.1 PASS`, `01.2 PASS`, `01.3 PASS`, `02.1 IN_PROGRESS`; all remaining listed prompts are `NOT_STARTED`. JSON count line: `{"IN_PROGRESS": 1, "NOT_STARTED": 68, "PASS": 3}`.

### F. Working tree snapshot

```cmd
git status --short
```

Exit code: `0`.

Actual output:

```text
 M .agent-run/prompt-state.json
 D Chatbot_Phap_luat_OpenCode_OMO_Repo_Pack.zip
?? docs/acceptance-criteria.md
?? docs/architecture/
?? docs/decision-log.md
?? docs/progress/01.2.md
?? docs/progress/01.3.md
?? docs/progress/02.1.md
?? docs/prompt-state.yaml
?? docs/prompt-transition-rules.md
?? docs/requirements-traceability-matrix.md
?? docs/scope-v1.md
```

Provenance: `.agent-run/prompt-state.json` is gate-managed state changed by legitimate `start 02.1`; the deleted ZIP and all untracked files other than this prompt's three documents pre-existed/fall outside this writer scope. `docs/architecture/` contains the two scoped architecture documents.

Product tests: `NOT_MEASURED`; đây là document validation, không là product evidence.

## Đối chiếu nguyên tắc cứng

- HR-01/02: chỉ Prompt 02.1; không chạy approval, submit hay prompt tiếp theo.
- HR-03/04: không viết code/contract/schema và không đổi boundary/owner đã chốt.
- HR-05/06/07: trạng thái có điều kiện, không ghi đè ngoài scope, có bằng chứng truy vết.
- HR-08..14: bounded context, single owner, private persistence isolation, no cross-DB, contract/protocol `NOT_DECIDED`, async/outbox điều kiện tương lai.
- HR-15..18/22: Document canonical/publish-active, Retrieval/Citation fail-closed, opaque request-bound context và lineage owner.
- HR-27: verified context không thay authorization tại domain service.
- HR-29..33: Feedback/Golden/Evaluation ownership không auto ground truth, training hay release.
- HR-41/42: nêu `DEC-005`, product test chưa đo và điều kiện còn lại.

## Sai lệch, rủi ro và điều kiện còn lại

Schema API/event, protocol, tech stack, storage engine và deployment packaging là `NOT_DECIDED`, thuộc 02.2–02.5. `DEC-005` OPEN là điều kiện feasibility/process; cần separately approved governance/manifest resolution, không phải boundary approval, trước executable Core path.

## Đề xuất prompt tiếp theo

Chỉ sau human approval: đề xuất Prompt 02.2. Không chạy trong phiên này.
