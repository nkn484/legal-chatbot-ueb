# Báo cáo Prompt 01.2

## Trạng thái đề xuất

`PASS_WITH_CONDITIONS_CANDIDATE`

Người dùng đã xác nhận miền core, chế độ retrieval-first không Provider và timebox 10 ngày dương lịch. Candidate có điều kiện vì REQ-RET-003 citation là Core MUST nhưng manifest hiện tại đặt 05.8 Citation sau 05.7 → 05.6 → 05.5 Provider, còn 05.6 yêu cầu real minimal Provider request, mâu thuẫn với Core không Provider/credential/cost. Điều kiện này phải được giải quyết bằng quyết định governance/manifest tương lai được phê duyệt riêng trước khi planning implementation downstream có thể khẳng định đường đi khả thi; Prompt 01.2 không sửa manifest/prompt tương lai và không defer citation. Danh sách corpus/approver, máy benchmark, ngày khởi động/demo và năng lực nhóm là chi tiết thực thi không đổi boundary/schema. Scope vẫn cần user-only approval sau submit; đây không phải tuyên bố product hoặc production readiness.

## Đã thay đổi

- `docs/scope-v1.md`: Khóa scope Core Demo, timebox nghiêm ngặt, persona, catalog `REQ-*`, fixture demo, NFR và điều kiện dependency citation.
- `docs/acceptance-criteria.md`: Map `AC-*`, kiểm tra timebox nghiêm ngặt và scope-lock condition của dependency citation; product acceptance vẫn `NOT_MEASURED`.
- `docs/requirements-traceability-matrix.md`: Ma trận mỗi `REQ-*` đúng một lần, gồm điều kiện dependency trên REQ-RET-003; không thay boundary hay owner.
- `docs/progress/01.2.md`: Báo cáo này, cập nhật `PASS_WITH_CONDITIONS_CANDIDATE`.

Writer không trực tiếp sửa service, source, contract, architecture, migration, test product hoặc state gate. Thay đổi `.agent-run/prompt-state.json` là gate-managed, được tạo/quan sát sau `python scripts/prompt_gate.py start 01.2`, và không được writer chỉnh thủ công; chỉ `Chatbot_Phap_luat_OpenCode_OMO_Repo_Pack.zip` là deletion có sẵn của người dùng được bảo toàn.

## Bằng chứng

- `git status --short` — baseline trước khi viết: exit code `0`; `.agent-run/prompt-state.json` là thay đổi gate-managed được tạo/quan sát sau `python scripts/prompt_gate.py start 01.2`, không do writer chỉnh thủ công; ` D Chatbot_Phap_luat_OpenCode_OMO_Repo_Pack.zip` là deletion có sẵn của người dùng được bảo toàn.
- Orchestrator đã inspect manifest, không thực thi chain: `prompts/manifest.json` dòng 815–913 định nghĩa dependency tuần tự 05.5 → 05.6 → 05.7 → 05.8; `prompts/groups/05_RAG_CHAT_PROVIDER.md` dòng 87–97 ghi 05.6 có “real minimal request”; dòng 119–131 mô tả 05.8 Citation. Đây là nguồn của điều kiện dependency, không phải Provider call.
- Final factual-cleanup `git diff --check` — exit code `0`; không có output, không phát hiện lỗi whitespace.
- `python -c "from pathlib import Path; import re,sys; names=('docs/scope-v1.md','docs/acceptance-criteria.md','docs/requirements-traceability-matrix.md','docs/progress/01.2.md'); paths={name:Path(name) for name in names}; missing=[name for name,path in paths.items() if not path.is_file()]; scope=paths[names[0]].read_text(encoding='utf-8') if not missing else ''; acceptance=paths[names[1]].read_text(encoding='utf-8') if not missing else ''; rtm=paths[names[2]].read_text(encoding='utf-8') if not missing else ''; reqs=sorted(set(re.findall(r'REQ-[A-Z0-9-]+',scope))); rows=[(cells[1],cells[2]) for line in scope.splitlines() for cells in [[cell.strip() for cell in line.split('|')]] if len(cells)>=4 and re.fullmatch(r'REQ-[A-Z0-9-]+',cells[1])]; missing_ac=[req for req in reqs if req not in acceptance]; missing_rtm=[req for req in reqs if req not in rtm]; invalid_priorities=[(req,priority) for req,priority in rows if priority not in {'MUST','SHOULD','LATER'}]; duplicate_rows=sorted({req for req,priority in rows if sum(1 for row_req,row_priority in rows if row_req==req)!=1}); print({'files_found':len(paths)-len(missing),'missing_files':missing,'req_count':len(reqs),'requirement_rows':len(rows),'missing_acceptance':missing_ac,'missing_rtm':missing_rtm,'invalid_priorities':invalid_priorities,'duplicate_requirement_rows':duplicate_rows,'exactly_one_priority_per_requirement':len(rows)==len(reqs) and not duplicate_rows and not invalid_priorities}); sys.exit(bool(missing or missing_ac or missing_rtm or invalid_priorities or duplicate_rows or len(rows)!=len(reqs)))"` — exit code `0`; output `{'files_found': 4, 'missing_files': [], 'req_count': 18, 'requirement_rows': 18, 'missing_acceptance': [], 'missing_rtm': [], 'invalid_priorities': [], 'duplicate_requirement_rows': [], 'exactly_one_priority_per_requirement': True}`.
- Final factual-cleanup `git status --short` — exit code `0`; output ` M .agent-run/prompt-state.json`, ` D Chatbot_Phap_luat_OpenCode_OMO_Repo_Pack.zip`, `?? docs/acceptance-criteria.md`, `?? docs/progress/01.2.md`, `?? docs/requirements-traceability-matrix.md`, `?? docs/scope-v1.md`.
- Product tests, Provider calls/paid services, gate lifecycle, commit và push: không chạy; product evidence là `NOT_MEASURED`. Orchestrator sở hữu reread, independent final validation và submit.

## Đối chiếu nguyên tắc cứng

- HR-01 đến HR-07: chỉ Prompt 01.2 và bốn artifact trong write scope; không chạy prompt kế tiếp, không code/contract/boundary change, ghi điều kiện dependency thay vì giả feasibility/PASS, và có traceability REQ→AC→RTM.
- HR-15 đến HR-23 liên quan: citation canonical REQ-RET-003 vẫn MUST, cùng publish filter, refusal thiếu evidence, không suy luận hiệu lực, document untrusted, immutability/provenance và không có secret/Provider trong core.
- HR-29, HR-30, HR-33, HR-34: Answer Improvement/fine-tuning được loại khỏi core và map `LATER` Group 06, không bị làm yếu.
- HR-39 đến HR-42: product tests/measurements ghi `NOT_MEASURED`; không có credential, production access, migration phá hủy, hay cam kết production.

Không xác định vi phạm blocker trong phạm vi documentation-only này; feasibility dependency của citation là điều kiện cần governance approval riêng, không được âm thầm defer.

## Sai lệch, rủi ro và điều kiện còn lại

- Scope-lock gate yêu cầu user approval và Orchestrator review; chưa có approval trong writer lane.
- Điều kiện bắt buộc: trước khi planning implementation downstream có thể khẳng định đường đi khả thi đến REQ-RET-003, phải có quyết định governance/manifest tương lai được phê duyệt riêng để xử lý conflict 05.8 → 05.7 → 05.6 → 05.5 với Core không Provider/credential/cost. Prompt 01.2 không sửa manifest/prompt và citation không bị defer.
- Danh sách corpus/approver, benchmark machine và demo dates/team capacity phải được giải quyết trước hoạt động tương ứng, không phải trước scope lock.
- Ngày khởi động được phê duyệt phải được xác định trước khi timebox 10 ngày liên tiếp bắt đầu; việc thiếu evidence trong timebox là `NOT_MEASURED` hoặc bị loại/defer.
- Stack Compose chính xác, privacy/retention implementation, boundary/schema/API/event chưa được quyết định; Prompt 02.1 sở hữu final boundary approval.
- Không có triển khai, benchmark hoặc product test; mọi acceptance sản phẩm là `NOT_MEASURED`.
- Frozen demo acceptance fixture có đúng 15 ca, không phải Golden Answer/training dataset và không đi vào Answer Improvement; 1 trong 3 ca đa nguồn phải refusal do evidence mâu thuẫn đáng kể.

## Đề xuất prompt tiếp theo

Sau khi người dùng phê duyệt Prompt 01.2, chỉ đề xuất Prompt 01.3; không chạy prompt đó. Scope approval vẫn chỉ do người dùng thực hiện sau submit.
