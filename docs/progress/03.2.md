# Báo cáo Prompt 03.2 — Runtime template cho backend service

## Trạng thái đề xuất

`PASS_CANDIDATE`

## Đã thay đổi

- `packages/workspace-tooling/templates/fastapi-service/`: thêm copy-only FastAPI template, dùng deterministic token substitution cho service name, module và environment prefix; template không là runtime shared library.
- `services/audit-service/`: là service duy nhất vượt skeleton, với runtime FastAPI service-local, settings Pydantic, lifespan async, pure-ASGI context/error middleware, health routes, HTTP client factory và read-only migration compatibility hook. Startup migration failure chỉ log event generic với error type, sau đó ném RuntimeError sanitized; không có AuditFact, DB, broker, auth, migration execution hoặc business route.
- `services/audit-service/requirements*.in|txt` và `pyproject.toml`: direct pins và hash lock runtime/dev; dev lock nay pin rõ `setuptools==83.0.0` và `wheel==0.46.2`, trùng build-system và được dùng cho `build --no-isolation`.
- `services/audit-service/tests/test_runtime.py`: 10 test unit/in-process ASGI, gồm lifecycle lặp, startup migration/client-factory failure canary safety, health, config fail-fast, response-start safety, request/error logs, timestamp/severity, identifiers/context reset, redaction và complete template parity.
- `services/audit-service/.gitignore` và template tương ứng: reserve caches/build output without deleting existing generated folders. `packages/workspace-tooling/workspace.py` và `tests/test_monorepo_skeleton.py` chỉ mở rộng fail-closed inventory/build rule cho audit-service runtime files; các component khác vẫn chỉ có skeleton files.

## Bằng chứng

Chi tiết exit code thực tế: `tests/evidence/03.2-validation.json`. Báo cáo JSON của `pip-audit`:

- `tests/evidence/03.2-pip-audit-runtime.json`
- `tests/evidence/03.2-pip-audit-dev.json`

Các lệnh pass (exit 0): regenerate dev hash lock, install `--require-hashes` trong fresh venv, xác nhận `setuptools==83.0.0`/`wheel==0.46.2`, 10 audit tests, Ruff check/format check, mypy strict (11 files), pip check, strict runtime/dev lockfile-only pip-audit (`--strict --require-hashes --disable-pip`) không có known vulnerabilities, wheel build `--no-isolation` từ temporary copy, workspace bootstrap/lint/test/build-all, monorepo skeleton tests (6), prompt-gate lifecycle tests (26), `verify_pack.py`, `git check-ignore` và `git diff --check`.

Wheel đã build là `audit_service-0.1.0-py3-none-any.whl` ở temporary output; không sinh `build/` hay `*.egg-info` trong repository.

## Đối chiếu nguyên tắc cứng

- HR-01/HR-02: chỉ Prompt 03.2; không submit/approve/reject/defer, commit, push hay chạy prompt tiếp theo.
- HR-03/HR-04/HR-10: ADR-003 ACCEPTED cho FastAPI/runtime độc lập; không đổi contract, API contract, event, schema, ADR hoặc service boundary. Health routes là runtime operational-only, không sửa public contract.
- HR-06/HR-13/HR-14: audit-service không import workspace tooling/service khác và không dùng shared domain/ORM/migration package; mọi runtime module/config/client là service-local. Các service/apps khác không bị thay đổi runtime.
- HR-23/HR-24/HR-26: không có secret required, settings không serialize, logs chỉ allowlist và redact key đệ quy; canary không xuất hiện trong startup exception, log hay response. HTTP client không gọi network, redirects bị tắt, timeout/limits bounded.
- HR-36/HR-37/HR-38: một startup boundary sanitized và finalizer đóng client/logger; typed config, liveness chỉ sau client construction, readiness sau migration gate, request/correlation/trace context, response-start-safe RFC-9457-style errors, RFC3339 UTC JSON timestamp và typed INFO/ERROR event severity đã được kiểm thử in-process.
- HR-39/HR-40/HR-41: unit/integration, parity, lint, strict typing, hash lock, pip check, pip-audit, wheel build và regression evidence đã chạy; limitations được nêu rõ.
- HR-40/HR-41: strict runtime/dev audit passed after authorized security patch; findings from previous vulnerable build pins are absent.

## Sai lệch, rủi ro và điều kiện còn lại

- Actual Uvicorn signal handling/network deployment là `NOT_MEASURED`; README chỉ ghi command có `--timeout-graceful-shutdown`.
- DB, migration execution, broker, auth, secrets, AuditFact/business logic, Docker và CI đều không được triển khai hoặc đo theo scope Prompt 03.2.
- Make targets là `NOT_MEASURED` vì local Windows không có `make`.
- No retry/circuit breaker, external runtime network call, hoặc production deployment được thêm. Template parity cố ý loại trừ generated hash lockfiles `requirements.txt` và `requirements-dev.txt`.
- `setuptools==83.0.0` and `wheel==0.46.2` are installed and used by `build --no-isolation`; strict runtime/dev audits report no known vulnerabilities.

## Đề xuất prompt tiếp theo

Chỉ sau human approval cho Prompt 03.2, đề xuất Prompt 03.3. Prompt 03.3 không được chạy trong phiên này.
