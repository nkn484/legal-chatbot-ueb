# Báo cáo Prompt 02.6 — privacy and threat model writer

## Trạng thái đề xuất

`PASS_WITH_CONDITIONS_CANDIDATE`

## Đã thay đổi

- `contracts/security/risk-register.yaml`: reconciled 41 planned risks, maps `RISK-038` to `LLM10`, maps the full API1..API10 set (including API6), moves live OIDC/JWKS risks to LATER, and separates future backup risk `RISK-041` from Core privacy risk `RISK-025`.
- `contracts/security/security-controls.yaml`: keeps broker mTLS/ACL producer authentication, distinguishes Core local/demo identity/workload mTLS from LATER live OIDC/JWKS, and splits Core minimization/deletion (`CTRL-PRIV-001`/`002`) from LATER backup/restore (`003`) and Provider privacy egress (`004`).
- `contracts/security/security-tests.yaml`: preserves 70 `PLANNED_NOT_RUN` fail-closed tests; OIDC/JWKS tests are LATER while workload identity remains Core.
- `contracts/security/privacy-data-map.yaml`: adds 18 classes, 18 retention rules, 15 encryption entries, exact DB mappings for 11 state-owning services, and 38 topology-checked flows.
- `contracts/security/validate_security_model.py` and `contracts/security/validation-report.json`: strict offline registry validator, topology/secret/backup checks, mutation evidence, and SHA-256 input freshness hashes.
- `docs/architecture/threat-model.md`, `docs/architecture/threat-data-flow.md`, `docs/architecture/privacy-model.md`: design-only threat, DFD, and privacy documentation.

## Bằng chứng

Commands run after writing:

```text
python contracts\security\validate_security_model.py --output contracts\security\validation-report.json
python -m py_compile contracts\security\validate_security_model.py
python -c "import yaml; from pathlib import Path; paths=['contracts/security/risk-register.yaml','contracts/security/security-controls.yaml','contracts/security/security-tests.yaml','contracts/security/privacy-data-map.yaml']; [yaml.safe_load(Path(p).read_text(encoding='utf-8')) for p in paths]; print('yaml-parse-ok', len(paths))"
python -c "from pathlib import Path; import re; paths=['contracts/security/risk-register.yaml','contracts/security/security-controls.yaml','contracts/security/security-tests.yaml','contracts/security/privacy-data-map.yaml','contracts/security/validate_security_model.py','docs/architecture/threat-model.md','docs/architecture/threat-data-flow.md','docs/architecture/privacy-model.md','docs/progress/02.6.md']; pat=re.compile(r'-----BEGIN [A-Z ]+PRIVATE KEY-----|(?i:(?:api[_-]?key|token|secret|password)\\s*[:=]\\s*[\\\"\\\']?[A-Za-z0-9+/=_-]{16,})'); hits=[p for p in paths if pat.search(Path(p).read_text(encoding='utf-8'))]; assert not hits, hits; print('secret-scan-ok', len(paths))"
python -c "from pathlib import Path; assert Path('docs/architecture/threat-data-flow.md').read_text(encoding='utf-8').count('```mermaid') == 1; print('mermaid-fence-ok')"
git diff --check
git status --short
```

Actual results are recorded in `contracts/security/validation-report.json`: 41 risks, 30 controls, 70 tests, 38 flows, 18 classes, 18 retention rules, 15 encryption entries, and 10/10 LATER flows denied (backup/DR 2, Provider 3, Feedback/Evaluation 2, source 3). The negative mutation suite rejects moving `SEC-PRIV-006` backup/restore testing to Core, adding a LATER test to a Core-applicable risk/control/retention rule, and attaching `SEC-PRIV-003` to Core `CTRL-PRIV-001`. All design validation and mutation checks passed. No submit, gate action, approval, commit, push, or network call was run.

## Đối chiếu nguyên tắc cứng

- HR-01/HR-02: only Prompt 02.6 artifacts were written; work stops at this candidate.
- HR-03/HR-04: no approved contract, schema, service boundary, or runtime behavior was altered.
- HR-15–HR-18: PUBLISHED/active eligibility, canonical citation, and refusal gates remain explicit.
- HR-23–HR-26: no secret literal; write-only provider secret design and default-denied external flows are required.
- HR-29–HR-35: feedback/dataset are LATER and not collected in Core; no auto-release.
- HR-36–HR-41: validation is deterministic and negative mutations demonstrate fail-closed registry checks.

## Sai lệch, rủi ro và điều kiện còn lại

The Councillor `BLOCKED` concern is represented as activation gates: a design candidate can pass conditionally while runtime remains blocked. No risk is accepted. Backup/DR is LATER `DENIED_NOT_SENT` and excluded from Core by `REQ-OPS-002`; `SEC-PRIV-006` is LATER-only, and backup encryption/restore evidence is `NOT_MEASURED`. Core `RISK-013` covers only object/version immutability; Core `RISK-035` covers only cross-service database/object-prefix isolation. KMS, mTLS, sandbox, AV, SSRF, broker ACL, scanners, deletion propagation, provider terms/DPA/training/retention/residency, live OIDC/SSO/JWKS, and lawful basis are `NOT_MEASURED`. Runtime tests remain `PLANNED_NOT_RUN`; actual implementation must not activate Critical/High capability until evidence exists. `DEC-005` remains OPEN.

## Đề xuất prompt tiếp theo

After human approval only, propose the approved implementation prompt that supplies the required runtime evidence; do not run it here.
