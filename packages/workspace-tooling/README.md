# Workspace tooling

Development-only Python standard-library tooling for the Prompt 03.1 skeleton. It validates boundaries and emits deterministic local skeleton ZIP artifacts; it is not a shared runtime package.
